{
  "name": "desarrollamo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "latest"
  }
}
